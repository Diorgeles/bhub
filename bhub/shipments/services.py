class Shipping:
    def handle_shipment_for_shipping(self, order):
        print(f"Pedido {order.id} - gerar uma guia de remessa para envio")

    def handle_duplicate_shipmentf_for_royalties(self, order):
        print(f"Pedido {order.id} - crie uma guia de remessa duplicada para o departamento de royalties")
